import React from 'react';
import { shallow } from 'enzyme';
import NumberOfEvents from '../NumberOfEvents';

describe('<NumberOfEvents /> component', () => {
    let NumOfEventsWrapper;
    beforeAll(() => {
        NumOfEventsWrapper = shallow(<NumberOfEvents />);
    });

    test
});